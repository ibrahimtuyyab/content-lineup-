node_modules/
dist/
.DS_Store
*.log
.shots/
.chrome-tmp/
data/*.db-wal
data/*.db-shm

# Secrets — never commit
.env
.env.local

# Build-time content cache
data/content-cache.json
data/pricing-cache.json
data/site-cache.json

# Vercel CLI link (project + org id, written by `vercel link`)
.vercel/

# Every .env variant is a secret — except the checked-in template.
.env*
!.env.example
